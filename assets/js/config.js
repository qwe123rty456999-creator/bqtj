/* ==========================================================================
   config.js — 站点配置（在 site.js 之前加载）
   ========================================================================== */

window.SITE = {
  name: 'bqtj.cc.cd',

  /* 文件存放位置（字幕 / 游戏包）
     留空          → 与网页同源，即 https://bqtj.cc.cd/files/...
     填完整网址    → 从对象存储/CDN 取，如 'https://files.bqtj.cc.cd'

     为什么留这个开关：Cloudflare Pages 单次部署最多 20000 个文件，
     字幕超过这个数就必须把 files/ 挪到 Cloudflare R2，届时只改这一行。 */
  fileBase: '',

  /* 首页「最新收录」显示几条 */
  latestCount: 6,
};
